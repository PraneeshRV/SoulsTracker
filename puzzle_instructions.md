Puzzle instructions

- Inspect the repository.
- If something looks missing, check the git history (`git log`, `git show`, `git grep`).
- The important next-step link is hidden in history in a file that was later deleted.

Good hunting.
